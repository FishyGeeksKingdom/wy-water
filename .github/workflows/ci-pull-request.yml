name: Publish Checks

on:
  pull_request:
    types: [opened, synchronize, reopened, edited, closed]
    branches:
      - main
    paths:
      - ".github/**"
jobs:
  Builds:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo Nothing To Build
