on:
  pull_request:
    types: [edited]
  needs: build
  runs-on: ubuntu-latest
  # Fresh checkout to protect from injection during the previous build step (like prepack)
  jobs:
    check_review:
      uses: ./.github/workflows/checkReviews.yml
      with:
        pr_id: ${{ github.event.pull_request.number }}
        owner: ${{ github.repository_owner }}
        name: "FishyGeeksKingdom"
    checkout_and_publish:
      steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-node@v1
          with:
            node-version: 14
        - name: Publish
          # VERY IMPORTANT NOT TO RUN SCRIPTS TO PROTECT FROM MALICIOUS PACKAGES
          run: |
            echo Maybe Success!
